This is a connection to the nano banana pro API. It removes all of the security flags to be blocked as little as possible, although it will not work every time.

You will need a GEMINI_API_KEY in your environment variables and to install the google gemini api python packages.
